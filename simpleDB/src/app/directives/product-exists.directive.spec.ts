import { ProductExistsDirective } from './product-exists.directive';

describe('ProductExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductExistsDirective();
    expect(directive).toBeTruthy();
  });
});
